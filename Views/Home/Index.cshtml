@{
    ViewBag.Title = "Home Page";
}
<script>
var responsiveSlider = function() {
var slider = document.getElementById("slider");
var sliderWidth = slider.offsetWidth;
var slideList = document.getElementById("slides");
var count = 1;
var items = slideList.querySelectorAll("li").length;
var prev = document.getElementById("prev");
var next = document.getElementById("next");
window.addEventListener('resize', function() {
 sliderWidth = slider.offsetWidth;
});
var prevSlide = function() {
if(count > 1) {
count = count - 2;
slideList.style.left = "-" + count * sliderWidth + "px";
count++;
}
else if(count = 1) {
count = items - 1;
slideList.style.left = "-" + count * sliderWidth + "px";
count++;
}
 };
 var nextSlide = function() {
if(count < items) {
slideList.style.left = "-" + count * sliderWidth + "px";
count++;
 }
 else if(count = items) {
slideList.style.left = "0px";
count = 1;
 }
};
next.addEventListener("click", function() {
 nextSlide();
});
prev.addEventListener("click", function() {
 prevSlide();
});
setInterval(function() {
 nextSlide()
}, 5000);
};
window.onload = function() {
responsiveSlider();
}
</script>
<div id="slider">
    <ul id="slides">
        <li><img src="..\..\Product_images\CPU\i3-12100(1).jpg" alt="Slide 1.jpg"></li>
        <li><img src="..\..\Product_images\CPU\i3-12100(2).jpg" alt="Slide 2.jpg"></li>
        <li><img src="..\..\Product_images\CPU\i3-12100(3).jpg" alt="Slide 3.jpg"></li>
        <li><img src="..\..\Product_images\CPU\i3-12400(3).jpg" alt="Slide 4.jpg"></li>
    </ul>
    <a id="prev" href="#">&#8810;</a>
    <a id="next" href="#">&#8811;</a>
</div>
<div>
    <h1>
        Text is placed here no spacing reason.
    </h1>
    <p>
        Bugs noted during session when you click next it automatic jump up for no reason,
        the debug didnt show anything.
    </p>
    <br />
    <br />
    <br />
</div>